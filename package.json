{
  "name": "storied",
  "version": "0.1.0",
  "source": "src/index.html",
  "browserslist": "> 0.5%, last 2 versions, not dead",
  "scripts": {
    "start": "parcel",
    "build": "parcel build --public-url https://www.storiedpgh.org",
    "test": "prettier . --check && stylelint \"**/*.scss\""
  },
  "devDependencies": {
    "@parcel/transformer-sass": "^2.12.0",
    "parcel": "^2.12.0",
    "prettier": "3.2.5",
    "stylelint": "^16.3.1",
    "stylelint-config-standard-scss": "^13.1.0"
  },
  "dependencies": {
    "normalize.css": "^8.0.1"
  }
}
